from django.apps import AppConfig


class ReferentielsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'referentiels'
    verbose_name = 'Référentiels (catégories, organisations, établissements)'
